apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubeeventjava-pipeline-sa
  labels:
    app.kubernetes.io/name: kubeeventjava
# The dockerhub-credentials Secret is referenced directly as a workspace in the
# PipelineRun, so no imagePullSecrets entry is required here.
# Add imagePullSecrets if the pipeline needs to pull private base images.
